export type TokenClient = google.accounts.oauth2.TokenClient;export type TokenClientConfig = google.accounts.oauth2.TokenClientConfig;
export type TokenResponse = google.accounts.oauth2.TokenResponse;

